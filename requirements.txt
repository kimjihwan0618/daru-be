# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- DB (SQLite 우선 → 추후 PostgreSQL 전환) ---
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
alembic==1.13.3
# PostgreSQL 전환 시 아래 두 줄 주석 해제 + requirements 재설치
# asyncpg==0.29.0
# pgvector==0.3.4

# --- 설정/검증 ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- 인증 ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- 외부 API 호출 ---
httpx==0.27.2

# --- 비동기 작업 ---
taskiq==0.11.13
taskiq-redis==0.5.6

# --- LLM/임베딩(추후 구현 시) ---
openai==1.51.0

# --- 개발/테스트 ---
pytest==8.3.3
pytest-asyncio==0.24.0
python-multipart==0.0.12
