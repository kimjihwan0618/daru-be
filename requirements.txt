# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- DB (SQLite 우선 → 추후 PostgreSQL 전환) ---
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
alembic==1.13.3
asyncpg==0.31.0
# pgvector==0.3.4  # pgvector 익스텐션 설치 후 활성화

# --- 설정/검증 ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- 인증 ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4가 bcrypt>=4.1과 호환되지 않는 알려진 버그 회피용 고정

# --- 외부 API 호출 ---
httpx==0.27.2

# --- 비동기 작업 ---
taskiq==0.11.13
taskiq-redis==0.5.6

# --- LLM/임베딩(추후 구현 시) ---
openai==1.51.0

# --- 개발/테스트 ---
pytest==8.3.3
pytest-asyncio==0.24.0
python-multipart==0.0.12
